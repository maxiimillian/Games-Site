import { useState } from "raect";

function Card(props) {
    const [value, setValue] = useState(0);
    const [suit, setSuite] = useState("blank");

    return (
        <div>
            <img src=""></img>
        </div>
    )
}

export default Card;