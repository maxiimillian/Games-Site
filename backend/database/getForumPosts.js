const PostModel = require("../models/Post");

module.exports = async function getFormPosts(tags = [], callback) {
    try {
        console.log("MAKING USER")
    } catch (err) {
        callback(err, null);
    }
}